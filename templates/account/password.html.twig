{% extends 'base.html.twig' %}

{% block title %}Modifier le mot de passe - Patco Boutique{% endblock %}

{% block content %}

    <h1>Modifier le mot de passe</h1>

    <a href="{{ path('account') }}">Retourner à mon compte...</a>
    
    <br/>
    <br/>

    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}

    {{ form(form) }}

{% endblock %}